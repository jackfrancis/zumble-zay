# Build artifacts and local files not needed in the image build context.
.git/
bin/
dist/
*.md
.env
.env.*
.vscode/
.idea/
.DS_Store
deploy/
# ...except the Ray-actors entrypoint, which Dockerfile.ray bakes into the image
# (docs/adr/0029).
!deploy/ray/llm_rank_ray.py
Dockerfile
.dockerignore
